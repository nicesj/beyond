rootProject.name = "beyond"

include("libbeyond")
include("libbeyond-android")
include("libbeyond-authenticator_ssl")
include("libbeyond-runtime_tflite")

for (project in rootProject.children) {
    project.projectDir = file("subprojects/${project.name}")
}
